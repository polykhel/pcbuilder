server:
  port: 8888

spring:
  application:
    name: pcb-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/polykhel/pcbuilder-config-store
        bootstrap: true
      label: main

encrypt:
  key-store:
    location: classpath:/config-server.jks
    password: o88D%70yfbX
    alias: config-server-key

management:
  endpoints:
    web:
      exposure:
        include: refresh
